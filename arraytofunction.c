#include <stdio.h>
#include <string.h>
#include <stdlib.h>

void add(int x);
void array

    int
    main(void)
{
    int a = 5;
    add(a);
    printf("a: %d\n", );

    return 0;
}

void add(int x)
{
    x = x + 1;
}